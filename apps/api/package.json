{
  "name": "api",
  "module": "src/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file .env --watch src/server.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "seed": "bun src/seed.ts",
    "migrate": "bun src/migrate.ts",
    "invites": "bun src/invites.ts",
    "set-password": "bun src/set-password.ts",
    "check": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@better-auth/cli": "^1.4.21",
    "@dashboard/shared-types": "workspace:*",
    "@libsql/client": "^0.18.0",
    "@libsql/kysely-libsql": "^0.4.1",
    "@sentry/bun": "^10.75.3",
    "@sentry/core": "10.75.3",
    "better-auth": "^1.7.6",
    "hono": "^4.13.9",
    "kysely": "^0.29.6",
    "node-cron": "^4.6.0",
    "zod": "^4.6.5"
  }
}
