# La web corre en el runtime de Workers (adapter @astrojs/cloudflare).
# Las variables de desarrollo van en .dev.vars (ver .dev.vars.example), no acá.
# En producción las vars están en wrangler.jsonc y los secrets se cargan desde GitHub Actions.

# Token para subir sourcemaps a Sentry en el build (opcional)
# SENTRY_AUTH_TOKEN=
