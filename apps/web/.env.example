SENTRY_ASTRO_TOKEN=TOKEN

# URL pública del frontend (usada por el middleware SSR para validar sesión contra Better Auth)
# En dev no es necesario (usa localhost:4321 por defecto)
# PUBLIC_FRONTEND_ORIGIN=https://dashboard.lucasramos.uy

# URL base de la API (en dev no es necesario, usa el proxy de Vite)
# PUBLIC_API_BASE=https://api.lucasramos.uy/api